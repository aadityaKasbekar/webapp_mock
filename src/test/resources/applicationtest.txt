# H2 Database settings for testing
spring.datasource.url=${SPRING_DATASOURCE_URL_TEST}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME_TEST}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME_TEST}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD_TEST}

# JPA settings for H2
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM_TEST}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO_TEST}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL_TEST}

# Disable any production-specific logging or features
logging.level.org.springframework=${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_TEST}
logging.level.com.cloudcomputing.movieRetrievalWebApp=${LOGGING_LEVEL_COM_CLOUDCOMPUTING_MOVIERETRIEVALWEBAPP_TEST}
